<h1> To Do List </h1> 


<form> 
  <% @items.each do |item| %>
  <%= item.name  %>: <%= item.description%>
  <br>
<% end %>
<%= form_tag ('/items', method: "post")    %>
<%= text_field_tag(:name) %>
<%= text_field_tag (:description) %>
<%= submit_tag("Create") %> 
</form> 